<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed == true ? "done!" : "do it!"%></p>
<p><%= link_to 'Modify task', edit_path(id: @task.id) %></p>
<p><%= link_to 'Return', tasks_path %></p>